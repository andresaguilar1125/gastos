# gastos
streamlit gastos app

## Run the Streamlit app ✅

Install dependencies:

```bash
pip install -r requirements.txt
```

Run locally:

```bash
streamlit run streamlit_app.py
```

The app is a minimal expense tracker demo (add expenses in the form, view the table and category totals).
